version: '3.8'

services:
  flask:
    build: ./flask_app
    container_name: flask
    expose:
      - "5000"

  nginx:
    image: nginx:stable
    container_name: nginx
    ports:
      - "80:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - flask
